# 按需质量检查

## 文字和布局疑点

比较源图与最终整页预览，重点看错字、漏字、金额/单位/日期、重复覆盖、容器越界与裁图底色。放大不清晰的区域。科研库页还要确认整图落在短图/长图黑框内，没有盖住标题或右侧栏，且未被拉扁。用户批准的原文或独立提取结果可作文字参照；不要只用模型自己转录的文字检查同一份输出。

已有 OCR 可辅助列出源图与 PPT 文本的差异，只让模型复核差异和低置信区域。OCR 也可能认错，不能自动用 OCR 覆盖源图文字。此版本未捆绑 OCR 引擎或自动逐字比对器。

`container_bbox` 只检查文本框是否超出指定容器。估算文字装不下、真实字体回退或换行改变时仍需查看实际渲染；避免用无条件缩小字体掩盖布局问题。

## 实际编辑验证

在最终交付文件中选一段原生文字，若有关键数值再选一个数值，准备 UTF-8 JSON 替换规则：

```json
[{"old":"当前标题","new":"编辑测试标题"},{"old":"12.5","new":"13.5"}]
```

运行随附工具：

```powershell
powershell.exe -NoProfile -File "<skill>/scripts/verify-edit.ps1" -DeckPath "<final.pptx>" -OutputDirectory "<verification-output>" -EditsPath "<edits.json>"
```

测试只修改独立副本，保存、重开后核对修改是否保留，并核对源文件哈希未改变。只交付未被测试修改的原文件。报告失败或替换规则未匹配时，不能宣称编辑验证通过。

这项验证覆盖选中的文字修改，不证明所有对象都可编辑，也不验证图表数据系列、公式或图片内部路径。按用户指定的编辑要求补做对应操作。复杂视觉保留为裁图时，明确内部不可编辑。

测试工具需要 Windows PowerPoint；无法运行时说明验证缺口。它不关闭用户的整个 PowerPoint 应用。macOS/Linux 上的 LibreOffice / PDF 预览只是非官方对照，不能写成 Office 验证通过。
