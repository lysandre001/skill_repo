# visualization

Two Agent Skills: `image-to-editable-ppt` and `image-to-figma`. Human install and short/long/Figma flow: [README.md](README.md).

Do not commit `.venv` or `__pycache__`. Keep `SKILL.md` frontmatter `name` equal to the folder name.
